version: 2

exposures:

  - name: weekly_metrics
    label: harshitha by the Week
    type: dashboard
    maturity: high
    description: >
      For more exposure about project details

    depends_on:
      - ref('final_business')
      - ref('order_append')
      - ref('emp_cust_data')
      - ref('orders')
      - ref('order_merge')
      - ref('order_delete_insert')
      - ref('order_insert_overwrite')
      - ref('emphr')
      - ref('customers')
      - ref('compare_table_rowcount')
      - ref('employees_salary')
      - ref('pre_post_model')
      - source('datafeed_shared_schema','raw_orders')
      - source('datafeed_shared_schema','customers')
      - source('datafeed_shared_schema','raw_customerdata')
      - source('datafeed_shared_schema','emp_sal')

    owner:
      name: harshitha
      email: harshithahanur646@gmail.com
